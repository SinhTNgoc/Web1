<div class="container">
  <h3 class="text-white">Testimonials</h3>
  <div class="row">
    {{#each data}}
      <div class="col">
        <p>{{summary}}</p>
        <p class="text-white">{{name}}</p>
        <a href={{email}}>{{email}}</a>
      </div>
    {{/each}}
  </div>
</div>