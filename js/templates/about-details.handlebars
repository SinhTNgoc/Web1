{{#each data}}
  <div class="container py-5">
    <h3 class="text-dark">{{name}}</h3>
    <div class="fix-height">
      <!-- <div class="left"> -->
      <!-- <div class="detail-img"> -->
      <img src="{{imagepath}}" alt="" class="round-img" />
      <!-- </div> -->
      {{{description}}}
    </div>
  </div>
{{/each}}
<!-- details -->